name: OWASP Dependency Check (Juice Shop)

on:
  workflow_dispatch:
  push:
  pull_request:
  create:
  delete:
  release:
  issues:
  issue_comment:
  schedule:
    - cron: "0 3 * * *"

jobs:
  dependency-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Instalar Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Baixar última versão do Dependency Check
        run: |
          wget https://github.com/dependency-check/DependencyCheck/releases/download/v12.2.1/dependency-check-12.2.1-release.zip

          unzip dependency-check-*.zip
          mv dependency-check /opt/dependency-check

      - name: Executar scan (Node / Juice Shop)
        run: |
          /opt/dependency-check/bin/dependency-check.sh \
            --project "Juice-Shop-Fork" \
            --scan package.json \
            --scan package-lock.json \
            --format ALL \
            --out dependency-check-report \
            --enableExperimental \
            --enableRetired

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: dependency-check-report
          path: dependency-check-report
